import styled from '@emotion/native'

import ContentContainer from '../content-container/ContentContainer.styled'

const S = {}

S.AddCard = styled.View`
  ${ContentContainer};
  justify-content: space-between;
`

export default S
